import 'package:flutter/material.dart';

class ActiveInstruction extends StatelessWidget {
  const ActiveInstruction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('TBD')
    );
  }
}
